---
phase: 02-detection-classification
plan: 04
type: execute
wave: 2
depends_on:
  - 02-03
files_modified:
  - lib/domain/models/category_mapping.dart
  - lib/data/services/classification_engine_service.dart
  - lib/data/repositories/category_repository.dart
  - lib/data/database/database_service.dart
autonomous: true

must_haves:
  truths:
    - "ML labels are mapped to user-defined categories with confidence scoring"
    - "Confidence threshold of 0.7 determines automatic vs manual sorting"
    - "Hierarchical categories are supported for nested organization"
    - "Category mappings persist in database with ML label associations"
  artifacts:
    - path: "lib/domain/models/category_mapping.dart"
      provides: "Category to ML label mapping model"
      min_lines: 60
    - path: "lib/data/services/classification_engine_service.dart"
      provides: "Classification engine with threshold logic"
      exports: ["classifyAndMatchCategory", "ClassificationDecision"]
    - path: "lib/data/repositories/category_repository.dart"
      provides: "Category CRUD with ML label mappings"
      exports: ["getCategories", "updateCategoryMappings", "getCategoryForLabels"]
  key_links:
    - from: "lib/data/services/classification_engine_service.dart"
      to: "lib/data/services/ml_classification_service.dart"
      via: "ML classification call"
      pattern: "mlClassificationService.classifyImage"
    - from: "lib/data/services/classification_engine_service.dart"
      to: "lib/data/repositories/category_repository.dart"
      via: "Category lookup by ML labels"
      pattern: "categoryRepository.getCategoryForLabels"
    - from: "lib/data/repositories/category_repository.dart"
      to: "lib/data/database/database_service.dart"
      via: "Category table queries"
      pattern: "db.query.*categories"
---

<objective>
Build classification engine that maps ML Kit labels to user-defined categories with confidence scoring. Implements confidence threshold (0.7) for automatic sorting vs manual review. Supports hierarchical categories and label-to-category mappings that persist in the database.

Purpose: Bridge between ML output (labels) and user organization (categories) - the brain of the auto-organization system.
Output: Classification engine that can match photos to categories and decide whether to auto-sort or queue for review.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-detection-classification/02-RESEARCH.md
@.planning/phases/01-foundation-data-safety/01-01-SUMMARY.md

# ML classification from Plan 02-03
@lib/data/services/ml_classification_service.dart
@lib/domain/models/classification_result.dart

# Existing category infrastructure from Phase 1
@lib/domain/models/category.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create category mapping models and update database schema</name>
  <files>
    - lib/domain/models/category_mapping.dart
    - lib/domain/models/ml_label_mapping.dart
    - lib/data/database/database_service.dart
  </files>
  <action>
    Create ml_label_mapping.dart:
    - MlLabelMapping model:
      - id: String (UUID)
      - categoryId: String (foreign key to categories)
      - label: String (ML Kit label like "Food", "Person", "Car")
      - weight: double (0.0-1.0, importance of this label for category)
      - confidenceThreshold: double (minimum confidence to consider, default 0.5)
      - createdAt: DateTime
    
    Create category_mapping.dart:
    - CategoryMatchResult class:
      - category: Category
      - confidence: double (calculated match confidence 0.0-1.0)
      - matchedLabels: List<String> (which ML labels contributed)
      - isAutoSortable: bool (confidence >= 0.7 threshold)
    - ClassificationDecision enum: auto_sort, manual_review, uncertain
    - CategoryHierarchy class (for CAT-04):
      - parentCategoryId: String?
      - depth: int (nesting level)
      - path: List<String> (breadcrumb)
    
    Update database_service.dart:
    - Add ml_label_mappings table:
      - id TEXT PRIMARY KEY
      - categoryId TEXT FOREIGN KEY (categories.id)
      - label TEXT NOT NULL
      - weight REAL DEFAULT 1.0
      - confidenceThreshold REAL DEFAULT 0.5
      - createdAt INTEGER
      - UNIQUE(categoryId, label)
    - Add index on ml_label_mappings.label for lookup performance
    - Add hierarchical fields to categories table (if not exists):
      - parentId TEXT (nullable, self-reference)
      - path TEXT (materialized path like "/Parent/Child")
    - Create migration from old schema if needed
    
    Per CAT-03: Categories map to ML labels with custom thresholds.
    Per CAT-04: Support hierarchical organization with parent-child relationships.
  </action>
  <verify>
    Database schema includes ml_label_mappings table with proper indices
    Category table has hierarchical support (parentId, path)
    MlLabelMapping and CategoryMatchResult models are defined
  </verify>
  <done>
    ml_label_mappings table exists, Category has hierarchical fields, MlLabelMapping model created, CategoryMatchResult model created
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement CategoryRepository with ML label management</name>
  <files>
    - lib/data/repositories/category_repository.dart
  </files>
  <action>
    Create/update category_repository.dart:
    - Existing methods from Phase 1: getCategories(), saveCategory(), deleteCategory()
    - New methods for ML mappings:
      - getCategoryLabelMappings(String categoryId): Future<List<MlLabelMapping>>
      - addLabelMapping(String categoryId, String label, double weight, double threshold)
      - removeLabelMapping(String categoryId, String label)
      - updateLabelWeight(String mappingId, double weight)
    - Methods for classification matching:
      - findMatchingCategories(List<ImageLabel> mlLabels): Future<List<CategoryMatchResult>>
        - Join ml_label_mappings with categories
        - For each category, calculate weighted confidence:
          - Match ML labels to mappings (case-insensitive)
          - Sum (label.confidence * mapping.weight) for all matches
          - Normalize by number of mappings to get final score
        - Sort by confidence DESC
        - Return top matches
      - getCategoryByLabel(String label): Future<Category?> (exact match)
    - Hierarchical category methods:
      - getChildCategories(String parentId): Future<List<Category>>
      - getCategoryHierarchy(String categoryId): Future<CategoryHierarchy>
      - moveCategory(String categoryId, String? newParentId)
    - Auto-discovery from learned labels:
      - suggestCategoriesFromLabels(List<String> labels): List<String>
        - Find common label patterns
        - Suggest category names based on frequent labels
    
    Per CAT-03: Each category can have multiple ML labels with different weights.
    Per ML-03: 0.7 threshold determines auto-sort eligibility.
  </action>
  <verify>
    Repository has CRUD for category ML label mappings
    findMatchingCategories calculates weighted confidence correctly
    Hierarchical queries work (parent-child relationships)
  </verify>
  <done>
    CategoryRepository has ML label mapping methods, findMatchingCategories with weighted scoring, hierarchical category support
  </done>
</task>

<task type="auto">
  <name>Task 3: Build ClassificationEngineService with threshold logic</name>
  <files>
    - lib/data/services/classification_engine_service.dart
    - lib/presentation/providers/classification_provider.dart
  </files>
  <action>
    Create classification_engine_service.dart:
    - classifyAndMatchCategory(String photoPath): Future<ClassificationMatch>
      - Step 1: Run ML classification (call MLClassificationService)
      - Step 2: Get top 10 ML labels from result
      - Step 3: Find matching categories (call CategoryRepository)
      - Step 4: Calculate final decision:
        - If top match confidence >= 0.7: ClassificationDecision.auto_sort
        - If top match confidence >= 0.5 but < 0.7: ClassificationDecision.manual_review
        - If top match confidence < 0.5: ClassificationDecision.uncertain
      - Step 5: Return ClassificationMatch with:
        - decision: ClassificationDecision
        - matchedCategory: Category? (if any match found)
        - confidence: double (0.0-1.0)
        - topMlLabels: List<ImageLabel> (top 5)
        - allCategories: List<CategoryMatchResult> (all matches sorted)
    
    - processClassificationQueue(): Future<void>
      - Get pending photos from PhotoQueueRepository
      - For each photo: classifyAndMatchCategory()
      - If auto_sort: schedule file move via WorkManager
      - If manual_review: update photo status, queue for notification
      - Update database with classification results
    
    - reclassifyPhoto(String photoId): Future<ClassificationMatch>
      - Re-run classification for a specific photo
      - Update results in database
    
    - getClassificationStats(): ClassificationStats
      - Total classified, auto-sorted, manual-review, failed counts
      - Average confidence scores
    
    Create classification_provider.dart (Riverpod):
    - StateNotifier for classification state
    - Methods: classifyPhoto(), processQueue(), getStats()
    - Expose stream of classification results for UI updates
    
    Per ML-03: 0.7 threshold for auto-sort (high confidence).
    Per ML-04: < 0.7 goes to manual review queue.
  </action>
  <verify>
    ClassificationEngineService has classifyAndMatchCategory method
    Confidence thresholds correctly determine auto_sort vs manual_review
    Service integrates with ML and repository layers
  </verify>
  <done>
    ClassificationEngineService exists with classifyAndMatchCategory, threshold logic (0.7 auto-sort), integration with ML and repository, ClassificationProvider exposes state to UI
  </done>
</task>

</tasks>

<verification>
1. Photo with matching ML labels returns correct category
2. High confidence (>= 0.7) results in auto_sort decision
3. Medium confidence (0.5-0.7) results in manual_review decision
4. Low confidence (< 0.5) results in uncertain decision
5. Multiple categories can match, sorted by confidence
6. Label weights affect final confidence calculation
7. Hierarchical categories are supported
8. ML label mappings persist in database
</verification>

<success_criteria>
- [ ] ML labels are correctly mapped to categories based on label mappings
- [ ] Category matching uses weighted confidence calculation
- [ ] Confidence >= 0.7 triggers auto_sort decision
- [ ] Confidence 0.5-0.7 triggers manual_review decision
- [ ] Confidence < 0.7 marks photo as uncertain
- [ ] Hierarchical categories work (parent-child relationships)
- [ ] ML label mappings can be added/removed/updated
- [ ] Classification results include top 5 ML labels for transparency
</success_criteria>

<output>
After completion, create `.planning/phases/02-detection-classification/02-04-SUMMARY.md`
</output>
