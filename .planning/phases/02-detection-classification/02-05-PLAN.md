---
phase: 02-detection-classification
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - lib/data/workers/photo_classification_worker.dart
  - lib/data/services/auto_sort_service.dart
  - lib/data/repositories/sort_operation_repository.dart
  - lib/presentation/providers/auto_sort_provider.dart
autonomous: true

must_haves:
  truths:
    - "High-confidence photos are automatically sorted to target folders"
    - "File moves use copy-verify-delete pattern for data safety"
    - "Transaction logging tracks every auto-sort operation"
    - "Failed auto-sorts are retried with exponential backoff"
  artifacts:
    - path: "lib/data/workers/photo_classification_worker.dart"
      provides: "WorkManager worker for background classification"
      min_lines: 120
    - path: "lib/data/services/auto_sort_service.dart"
      provides: "Auto-sort coordination service"
      exports: ["executeAutoSort", "SortOperationResult"]
    - path: "lib/data/repositories/sort_operation_repository.dart"
      provides: "Repository for sort operations"
      exports: ["logSortOperation", "getPendingSorts", "markSortComplete"]
  key_links:
    - from: "lib/data/workers/photo_classification_worker.dart"
      to: "lib/data/services/classification_engine_service.dart"
      via: "Classification in worker"
      pattern: "classificationEngine.classifyAndMatchCategory"
    - from: "lib/data/services/auto_sort_service.dart"
      to: "lib/data/services/safe_file_service.dart"
      via: "Safe file move via SAF"
      pattern: "safeFileService.movePhoto"
    - from: "lib/data/services/auto_sort_service.dart"
      to: "lib/data/repositories/transaction_repository.dart"
      via: "Transaction logging"
      pattern: "transactionRepository.logOperation"
---

<objective>
Implement WorkManager-based background classification and auto-sort execution. High-confidence photos (≥0.7) are automatically moved to target folders using safe file operations with transaction logging. Failed operations are queued for retry.

Purpose: Complete the automatic organization pipeline - from detection to classification to file movement.
Output: Background workers that process photos and auto-sort them without user intervention.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-detection-classification/02-RESEARCH.md
@.planning/phases/01-foundation-data-safety/01-03-SUMMARY.md

# Safe file operations from Phase 1
@lib/data/services/safe_file_service.dart
@lib/data/repositories/transaction_repository.dart

# Classification engine from Plan 02-04
@lib/data/services/classification_engine_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PhotoClassificationWorker for WorkManager</name>
  <files>
    - lib/data/workers/photo_classification_worker.dart
    - lib/data/workers/worker_registration.dart
    - android/app/src/main/AndroidManifest.xml
  </files>
  <action>
    Create photo_classification_worker.dart:
    - PhotoClassificationWorker class with @pragma('vm:entry-point')
    - WorkManager callback function:
      - Execute task with photoPath from inputData
      - Initialize dependencies (database, services)
      - Call classificationEngine.classifyAndMatchCategory(photoPath)
      - Handle result:
        - If auto_sort: trigger file move via AutoSortService
        - If manual_review: update photo status, don't move
        - If uncertain: mark as needs attention
      - Return Future.value(true) for success, false for retry
    - Constraints configuration:
      - requiresBatteryNotLow: true (MON-05)
      - requiresStorageNotLow: true
      - networkType: NetworkType.notRequired (on-device ML)
    - Error handling:
      - Try-catch around entire operation
      - Log errors for debugging
      - Return false to trigger WorkManager retry
      - Limit retries to 3 attempts with exponential backoff
    
    Create worker_registration.dart:
    - registerClassificationWorker(): Initialize WorkManager
    - scheduleClassification(String photoPath): Register one-off task
      - unique name: "classify-${photoPath.hashCode}"
      - existingWorkPolicy: ExistingWorkPolicy.keep (prevent duplicates)
      - initialDelay: Duration(seconds: 2) (debounce rapid changes)
    - schedulePeriodicQueueProcessing(): Background queue checker
      - Every 15 minutes when constraints met
    - cancelClassification(String photoPath): Cancel pending task
    
    Update AndroidManifest.xml:
    - Ensure WorkManager service declarations are present
    - Already have RECEIVE_BOOT_COMPLETED for auto-restart
    
    Per RESEARCH.md: Use WorkManager for ML processing, not foreground service.
    Per MON-05: Battery constraints prevent drain during low battery.
  </action>
  <verify>
    Worker class has @pragma('vm:entry-point') annotation
    WorkManager constraints include battery and storage checks
    Registration function exists for scheduling tasks
  </verify>
  <done>
    PhotoClassificationWorker exists with proper entry point, WorkManager scheduling configured with battery constraints, duplicate prevention via ExistingWorkPolicy.keep
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement AutoSortService with safe file operations</name>
  <files>
    - lib/data/services/auto_sort_service.dart
    - lib/domain/models/sort_operation.dart
  </files>
  <action>
    Create sort_operation.dart model:
    - SortOperation class:
      - id: String (UUID)
      - photoPath: String
      - sourceFolder: String
      - targetFolder: String
      - categoryId: String
      - confidence: double
      - status: SortStatus enum (pending, copying, verifying, completed, failed, rolled_back)
      - errorMessage: String?
      - createdAt: DateTime
      - completedAt: DateTime?
      - transactionId: String (links to TransactionLog)
    
    Create auto_sort_service.dart:
    - executeAutoSort(SortOperation operation): Future<SortResult>
      - Step 1: Validate source file exists
      - Step 2: Update operation status to COPYING
      - Step 3: Call safeFileService.movePhoto() with:
        - sourcePath: operation.photoPath
        - targetFolderUri: category.targetFolderUri
        - categoryId: operation.categoryId
      - Step 4: Wait for move completion
      - Step 5: If success:
        - Update operation status to COMPLETED
        - Update photo metadata in database
        - Return success result
      - Step 6: If failure:
        - Update operation status to FAILED
        - Log error message
        - Schedule retry via WorkManager
        - Return failure result
    - canAutoSort(String photoPath): Future<bool>
      - Check photo exists
      - Check target folder has permission
      - Check storage space available
      - Check no existing operation pending for this photo
    - rollbackSort(String operationId): Future<bool>
      - For failed operations, attempt to restore file
      - Use transaction log for recovery
    - getSortStatistics(): SortStats
      - Counts by status, average processing time
    
    Integrate with existing SafeFileService from Phase 1:
    - Reuse copy-verify-delete transaction pattern
    - Log to TransactionRepository for crash recovery
    
    Per FILE-02: Use copy-then-verify-then-delete pattern.
    Per ERR-01: Failed operations retry with exponential backoff.
  </action>
  <verify>
    AutoSortService integrates with SafeFileService
    SortOperation model has all required fields
    executeAutoSort handles success and failure paths
  </verify>
  <done>
    AutoSortService exists with executeAutoSort using SafeFileService, SortOperation model defined, rollback capability implemented
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SortOperationRepository and wire up end-to-end flow</name>
  <files>
    - lib/data/repositories/sort_operation_repository.dart
    - lib/data/database/database_service.dart
    - lib/main.dart
    - lib/presentation/providers/auto_sort_provider.dart
  </files>
  <action>
    Update database_service.dart:
    - Add sort_operations table:
      - id TEXT PRIMARY KEY
      - photoPath TEXT NOT NULL
      - sourceFolder TEXT
      - targetFolder TEXT
      - categoryId TEXT
      - confidence REAL
      - status TEXT (SortStatus enum)
      - errorMessage TEXT
      - createdAt INTEGER
      - completedAt INTEGER
      - transactionId TEXT
      - INDEX on status + createdAt
      - INDEX on photoPath (prevent duplicates)
    
    Create sort_operation_repository.dart:
    - createSortOperation(SortOperation): Insert new operation
    - getPendingSorts({int limit = 10}): Get pending operations
    - getSortByPhotoPath(String path): Check for existing operation
    - markAsCopying(String id): Update status
    - markAsCompleted(String id, String transactionId): Success
    - markAsFailed(String id, String error): Failure with message
    - getSortHistory({DateTime? since}): Get completed/failed operations
    - clearOldCompleted(Duration olderThan): Cleanup
    
    Update main.dart:
    - Register WorkManager callback in main()
    - Initialize worker registration on app startup
    - Ensure proper initialization order:
      1. Database
      2. WorkManager
      3. Foreground service
    
    Create auto_sort_provider.dart (Riverpod):
    - StateNotifier for auto-sort state
    - Methods: processPhoto(), getQueueStatus(), retryFailed()
    - Stream of sort operations for UI
    - Badge count for pending sorts
    
    Wire up ClassificationEngineService to trigger auto-sort:
    - When classifyAndMatchCategory returns auto_sort:
      - Create SortOperation
      - Call autoSortService.executeAutoSort()
      - Update UI via provider
    
    Per ERR-01: Schedule retries for failed operations.
    Per ERR-04: Transaction logging enables crash recovery.
  </action>
  <verify>
    sort_operations table exists in database
    Repository has CRUD for sort operations
    End-to-end test: detect photo → classify → auto-sort
    Failed operations are logged and can be retried
  </verify>
  <done>
    SortOperationRepository exists, database has sort_operations table, end-to-end flow works (detection → classification → auto-sort), failed operations retried
  </done>
</task>

</tasks>

<verification>
1. Photo detected → queued → classified → auto-sorted (high confidence)
2. File move uses copy-verify-delete pattern
3. Transaction log records the operation
4. WorkManager constraints respected (battery not low)
5. Failed operations are retried (max 3 attempts)
6. Duplicate photos not processed twice
7. Sort status visible in UI (pending/copying/completed)
8. Manual review photos not auto-moved
</verification>

<success_criteria>
- [ ] Photos with >= 0.7 confidence are automatically moved to target folders
- [ ] File moves use copy-verify-delete pattern for safety
- [ ] Transaction logging tracks all auto-sort operations
- [ ] WorkManager schedules jobs with battery constraints
- [ ] Failed sorts are retried up to 3 times with exponential backoff
- [ ] Sort operation status is visible in UI (provider state)
- [ ] Manual review photos (< 0.7) are not auto-moved
- [ ] Duplicate processing is prevented via ExistingWorkPolicy.keep
</success_criteria>

<output>
After completion, create `.planning/phases/02-detection-classification/02-05-SUMMARY.md`
</output>
