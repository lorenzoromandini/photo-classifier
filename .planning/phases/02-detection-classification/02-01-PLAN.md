---
phase: 02-detection-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/data/services/photo_monitor_service.dart
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/kotlin/com/example/photo_classifier/PhotoMonitorService.kt
  - android/app/src/main/kotlin/com/example/photo_classifier/MediaStoreObserver.kt
  - lib/data/platform/media_store_channel.dart
autonomous: true

must_haves:
  truths:
    - "Foreground service maintains persistent notification when active"
    - "MediaStore ContentObserver detects new photos in monitored directories"
    - "Service restarts automatically after device reboot"
    - "Service handles Doze mode gracefully without crashing"
  artifacts:
    - path: "pubspec.yaml"
      provides: "flutter_foreground_task dependency added"
      contains: "flutter_foreground_task: ^9.2.1"
    - path: "lib/data/services/photo_monitor_service.dart"
      provides: "TaskHandler for foreground service"
      min_lines: 100
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Foreground service and boot receiver permissions"
      contains: "FOREGROUND_SERVICE_DATA_SYNC"
    - path: "lib/data/platform/media_store_channel.dart"
      provides: "Platform channel to MediaStore observer"
      exports: ["startMediaStoreObserver", "stopMediaStoreObserver", "onNewPhotoDetected"]
  key_links:
    - from: "lib/main.dart"
      to: "FlutterForegroundTask.init"
      via: "service initialization in main()"
      pattern: "FlutterForegroundTask.init"
    - from: "lib/data/services/photo_monitor_service.dart"
      to: "MethodChannel('photo_classifier/media_store')"
      via: "platform channel for MediaStore communication"
      pattern: "MethodChannel.*media_store"
---

<objective>
Set up persistent foreground service with MediaStore ContentObserver for automatic photo detection. The service maintains a notification, monitors DCIM/Camera and WhatsApp directories for new photos, and restarts automatically after device reboot.

Purpose: This is the foundation of the automatic photo detection pipeline - without it, no photos can be detected for processing.
Output: Working foreground service that detects new photos and notifies Flutter via platform channel.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-detection-classification/02-RESEARCH.md
@.planning/phases/01-foundation-data-safety/01-05-SUMMARY.md

# Phase 1 provides ML Kit foundation
@lib/data/services/ml_labeling_service.dart

# Existing database for photo tracking
@lib/data/database/database_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flutter_foreground_task dependency and update AndroidManifest</name>
  <files>
    - pubspec.yaml
    - android/app/src/main/AndroidManifest.xml
    - android/app/build.gradle
  </files>
  <action>
    Add `flutter_foreground_task: ^9.2.1` to pubspec.yaml dependencies (already have workmanager, google_mlkit_image_labeling).
    
    Update AndroidManifest.xml to include:
    - FOREGROUND_SERVICE permission
    - FOREGROUND_SERVICE_DATA_SYNC permission (required for Android 14+)
    - RECEIVE_BOOT_COMPLETED for auto-restart
    - REQUEST_IGNORE_BATTERY_OPTIMIZATIONS for Doze handling
    - Service declaration for flutter_foreground_task with dataSync type
    - Boot receiver configuration for auto-restart
    
    Per RESEARCH.md: Android 15+ has 6-hour timeout for dataSync - service must be restartable.
    
    Also update android/app/build.gradle to ensure compileSdkVersion >= 35 for flutter_foreground_task v9.x compatibility.
  </action>
  <verify>
    Run `flutter pub get` succeeds without errors
    Check AndroidManifest.xml contains all required permissions and service declarations
  </verify>
  <done>
    pubspec.yaml has flutter_foreground_task ^9.2.1, AndroidManifest.xml has all required permissions and service declarations, compileSdkVersion >= 35
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MediaStore ContentObserver platform channel</name>
  <files>
    - android/app/src/main/kotlin/com/example/photo_classifier/MediaStoreObserver.kt
    - android/app/src/main/kotlin/com/example/photo_classifier/MainActivity.kt
    - lib/data/platform/media_store_channel.dart
  </files>
  <action>
    Create MediaStoreObserver.kt (Kotlin) that:
    - Extends ContentObserver with Handler(Looper.getMainLooper())
    - Monitors MediaStore.Images.Media.EXTERNAL_CONTENT_URI
    - Queries for new images since last check using DATE_ADDED > timestamp
    - Filters for monitored directories: /DCIM/Camera, /Download, /WhatsApp/Media/WhatsApp Images
    - Sends detected photo paths to Flutter via EventChannel/MethodChannel
    - Persists last check timestamp in SharedPreferences
    
    Update MainActivity.kt to:
    - Register the MediaStore observer platform channel
    - Set up EventChannel for streaming photo detection events
    
    Create media_store_channel.dart (Flutter side):
    - EventChannel wrapper for receiving photo detection events
    - StreamController for broadcasting new photo events
    - Methods to start/stop MediaStore observation
    - Error handling for platform channel failures
    
    Per RESEARCH.md: Use ContentObserver (not polling) for event-driven detection.
    Handle URI permission loss after reboot - re-initialize monitoring in service onStart.
  </action>
  <verify>
    App compiles without Kotlin/Dart errors
    MediaStoreObserver registers ContentObserver on EXTERNAL_CONTENT_URI
    Platform channel methods exist for start/stop observation
  </verify>
  <done>
    MediaStoreObserver.kt exists with ContentObserver implementation, MainActivity.kt registers platform channel, media_store_channel.dart provides Flutter API for observation control
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement foreground service with boot auto-restart</name>
  <files>
    - lib/data/services/photo_monitor_service.dart
    - lib/main.dart
    - lib/presentation/providers/monitoring_provider.dart
  </files>
  <action>
    Create photo_monitor_service.dart:
    - PhotoMonitorTaskHandler extending TaskHandler from flutter_foreground_task
    - onStart(): Initialize MediaStore observer via platform channel, request battery optimization exemption
    - onRepeatEvent(): Health check (empty or log heartbeat)
    - onDestroy(): Cancel MediaStore subscription, cleanup
    - onReceiveData(): Handle commands from UI (pause/resume monitoring)
    - Send detected photos to Flutter via sendDataToMain
    - Handle Doze mode gracefully - log when entering/leaving Doze
    
    Update main.dart:
    - Initialize FlutterForegroundTask.initCommunicationPort() before runApp
    - Initialize service config with notification options and foregroundTaskOptions
    - Set autoRunOnBoot: true per RESEARCH.md for MON-03
    - Set eventAction: ForegroundTaskEventAction.repeat(60000) for health checks
    
    Create monitoring_provider.dart (Riverpod):
    - StateNotifier for monitoring state (active/inactive/paused)
    - Methods to start/stop service with notification permission handling
    - Stream subscription for photo detection events from service
    - Integration with database to queue detected photos
    
    Per RESEARCH.md: Service should only monitor and queue, not process ML (use WorkManager for that to avoid ANR).
  </action>
  <verify>
    App builds and runs
    Service starts with notification showing "Watching for new photos..."
    Taking a photo in Camera app triggers detection within 5 seconds
    Rebooting device restarts service automatically
  </verify>
  <done>
    PhotoMonitorTaskHandler exists with MediaStore observation, service shows persistent notification, auto-restarts on boot, detected photos sent to Flutter via platform channel
  </done>
</task>

</tasks>

<verification>
1. Foreground service notification appears and stays persistent
2. New photos in DCIM/Camera detected within 5 seconds
3. New photos in WhatsApp/Media detected
4. Service survives device reboot and restarts automatically
5. Service continues running when device enters Doze mode
6. No ANR or crash during 6-hour runtime window
</verification>

<success_criteria>
- [ ] Foreground service notification shows "Photo Monitor - Watching for new photos..."
- [ ] MediaStore ContentObserver detects new JPEG/PNG photos within 5 seconds
- [ ] Service restarts automatically within 30 seconds after device reboot
- [ ] Service continues monitoring without crash during Doze mode
- [ ] Detected photo paths are sent to Flutter via platform channel
</success_criteria>

<output>
After completion, create `.planning/phases/02-detection-classification/02-01-SUMMARY.md`
</output>
