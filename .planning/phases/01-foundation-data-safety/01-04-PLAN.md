---
phase: 01-foundation-data-safety
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - lib/presentation/screens/onboarding_screen.dart
  - lib/presentation/providers/onboarding_provider.dart
  - lib/presentation/widgets/permission_card.dart
  - lib/presentation/widgets/folder_discovery_progress.dart
  - lib/data/services/onboarding_service.dart
  - android/app/src/main/AndroidManifest.xml
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Onboarding is single-screen (not multi-step wizard)"
    - "SAF permissions requested with context explanation"
    - "Folder discovery starts after permissions granted"
    - "Progress shown while discovering folders in Pictures/"
    - "Onboarding completes to main screen after discovery"
  artifacts:
    - path: "lib/presentation/screens/onboarding_screen.dart"
      provides: "Single-screen onboarding UI"
      min_lines: 100
    - path: "lib/presentation/providers/onboarding_provider.dart"
      provides: "Onboarding state management"
      min_lines: 80
    - path: "lib/data/services/onboarding_service.dart"
      provides: "Onboarding business logic"
      min_lines: 60
  key_links:
    - from: "OnboardingProvider"
      to: "FolderRepository"
      via: "calls discoverAndSyncFolders() after permissions"
    - from: "OnboardingScreen"
      to: "file_picker"
      via: "FilePicker.platform.getDirectoryPath() for SAF picker"
---

<objective>
Build single-screen onboarding flow per user decision. Screen explains app value, requests SAF permissions with context, discovers folders in Pictures/, shows progress, and completes to main screen. No multi-step wizard.

Purpose: Required for FILE-01 (SAF permissions) and CAT-01 (auto-discover categories). First impression - must be simple and clear.

Output:
1. OnboardingScreen with single-screen design
2. OnboardingProvider for state management
3. OnboardingService for business logic
4. Permission request with SAF picker
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-safety/01-CONTEXT.md
@.planning/phases/01-foundation-data-safety/01-01-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-02-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-03-SUMMARY.md

# Existing Flutter onboarding
@/home/lromandini/projects/photo-classificator/lib/presentation/screens/onboarding_screen.dart
@/home/lromandini/projects/photo-classificator/lib/presentation/providers/onboarding_provider.dart
@/home/lromandini/projects/photo-classificator/lib/data/services/preference_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Onboarding State and Widgets</name>
  <files>
    lib/presentation/providers/onboarding_provider.dart
    lib/presentation/widgets/permission_card.dart
    lib/presentation/widgets/folder_discovery_progress.dart
    lib/domain/models/onboarding_state.dart
  </files>
  <action>
    Create onboarding state management and UI components:

    1. Create `onboarding_state.dart`:
       ```dart
       enum OnboardingStage { welcome, requestingPermission, discovering, complete, error }
       
       class OnboardingState {
         final OnboardingStage stage;
         final int discoveredFolders;
         final int totalPhotos;
         final String? errorMessage;
         final bool isLoading;
         
         const OnboardingState({
           this.stage = OnboardingStage.welcome,
           this.discoveredFolders = 0,
           this.totalPhotos = 0,
           this.errorMessage,
           this.isLoading = false,
         });
       }
       ```

    2. Update `onboarding_provider.dart`:
       - Convert to StateNotifier or use Riverpod's AsyncNotifier
       - State: AsyncValue<OnboardingState>
       - Methods:
         - requestPermission() - shows SAF picker via file_picker
         - onPermissionGranted(String folderUri) - persists URI, starts discovery
         - onPermissionDenied() - sets error state with retry option
         - startDiscovery() - calls FolderRepository.discoverAndSyncFolders()
         - completeOnboarding() - saves to preferences, navigates
       - Handle loading states properly

    3. Create `permission_card.dart`:
       - PermissionCard widget with:
         - Icon (Icons.folder_open)
         - Title: "Access Your Photos"
         - Description explaining WHY (per user decision)
         - "Grant Access" button
       - Material 3 Card styling
       - Shows error state if permission denied

    4. Create `folder_discovery_progress.dart`:
       - FolderDiscoveryProgress widget:
         - CircularProgressIndicator or LinearProgressIndicator
         - Text: "Found $count folders"
         - Optional: "Scanning..." animated text
       - Material 3 styling

    Design requirements (user decision - single screen):
       - Value explanation at top
       - Permission card with context
       - Progress indicator for discovery
       - No multi-step wizard, no tabs, no pages
  </action>
  <verify>
    Run `flutter analyze` - no errors.
    Verify widgets compile by building:
    - flutter build apk --debug
    Check previews if using @widgetbook or similar.
  </verify>
  <done>
    OnboardingState defined with all stages.
    OnboardingProvider manages state transitions.
    PermissionCard and FolderDiscoveryProgress widgets created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement OnboardingService</name>
  <files>
    lib/data/services/onboarding_service.dart
  </files>
  <action>
    Create OnboardingService for business logic:

    1. Create `onboarding_service.dart`:
       - OnboardingService with dependencies:
         - PreferenceService (for persistence)
         - SafService (for folder discovery)
         - FolderRepository (for database sync)
       - Methods:
         - Future<Result<String>> requestPicturesFolder()
           - Uses file_picker to show SAF picker
           - Returns folder URI on success
         - Future<Result<void>> persistPermission(String uri)
           - Calls platform channel to persist SAF permission
         - Future<Result<List<FolderModel>>> discoverFolders(String baseUri)
           - Calls FolderRepository.discoverAndSyncFolders()
         - Future<void> completeOnboarding()
           - Sets onboardingComplete = true in preferences
           - Sets firstLaunch = false
         - Future<bool> hasCompletedOnboarding()
           - Reads from preferences

    2. Error handling:
       - Return Result<T> for all operations
       - Permission denied: Result.failure("Permission denied. Please grant access to continue.")
       - Discovery failed: Result.failure("Failed to discover folders: $error")

    3. Per user decision (01-CONTEXT.md):
       - Permission response handling: Show spinner while waiting
       - Post-permission flow: Success confirmation before main screen
       - Re-onboarding trigger: If permissions revoked later

    Implements:
    - FILE-01: SAF obtains permissions for target folders
    - ERR-03: Permission denials trigger re-onboarding (detected on startup)
  </action>
  <verify>
    Test methods individually:
    ```dart
    final service = OnboardingService(...);
    final result = await service.requestPicturesFolder();
    result.when(
      success: (uri) => print('Selected: $uri'),
      failure: (error) => print('Error: $error'),
    );
    ```
  </verify>
  <done>
    OnboardingService has all required methods.
    Result<T> pattern used for error handling.
    Permission and discovery workflows working.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update OnboardingScreen with Complete Flow</name>
  <files>
    lib/presentation/screens/onboarding_screen.dart
    android/app/src/main/AndroidManifest.xml
  </files>
  <action>
    Update existing OnboardingScreen with complete single-screen flow:

    1. Update `onboarding_screen.dart`:
       - Use ConsumerWidget from Riverpod
       - Watch onboardingProvider for state
       - Build different UI based on stage:

       **WELCOME stage:**
       - App icon/logo (Icons.photo_library, size 80)
       - Title: "Photo Classifier"
       - Description: "Automatically organize your photos using on-device machine learning. Your photos stay private and never leave your device."
       - PermissionCard (from Task 1)
       - "Grant Access" button

       **REQUESTING_PERMISSION stage:**
       - Show CircularProgressIndicator
       - Text: "Opening folder picker..."
       - Uses file_picker for SAF

       **DISCOVERING stage:**
       - Show FolderDiscoveryProgress
       - Text: "Found ${state.discoveredFolders} folders"
       - Optional: "Scanning ${state.currentFolder}..."

       **COMPLETE stage:**
       - Checkmark icon
       - Text: "Ready to organize!"
       - Auto-navigate to /main after 1 second delay
       - OR "Continue" button for manual advance

       **ERROR stage:**
       - Error icon
       - Error message from state
       - "Try Again" button

    2. Update `AndroidManifest.xml`:
       - Add permissions:
         ```xml
         <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
         <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
         <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
         ```
       - Add intent-filter for MAIN/LAUNCHER (existing)
       - No MANAGE_EXTERNAL_STORAGE per user constraints (use SAF only)

    3. Navigation:
       - On complete: Navigator.of(context).pushReplacementNamed('/main')
       - Ensure MainScreen route exists in main.dart

    4. Per user decision (01-CONTEXT.md):
       - Single-screen only (no wizard, no tabs, no pages)
       - Permission context explanation before request
       - Spinner during permission response
       - Success confirmation before main screen
  </action>
  <verify>
    Run on Android device/emulator:
    1. Fresh install, launch app
    2. See onboarding screen with value explanation
    3. Tap "Grant Access" - SAF picker opens
    4. Select Pictures folder
    5. See discovery progress
    6. See "Complete" screen
    7. Auto-navigate to main screen
    8. Kill app, restart - should go to main (onboarding complete)
  </verify>
  <done>
    OnboardingScreen shows all stages correctly.
    SAF picker opens on permission request.
    Folder discovery shows progress.
    Navigation to main screen works.
    State persists across restarts.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify Onboarding UX Flow</name>
  <what-built>
    Complete onboarding flow implemented:
    - Single-screen onboarding UI
    - SAF permission picker integration
    - Folder discovery with progress
    - Navigation to main screen
    - State persistence
  </what-built>
  <how-to-verify>
    **Test Steps:**

    1. **Fresh Install Test:**
       - Uninstall app: `adb uninstall com.example.photo_classifier`
       - Install APK: `adb install build/app/outputs/flutter-apk/app-debug.apk`
       - Launch app
       - **Expected:** See onboarding screen (not main screen)

    2. **Permission Flow Test:**
       - Tap "Grant Access"
       - **Expected:** SAF picker opens (Android system folder picker)
       - Select "Pictures" folder
       - **Expected:** Return to app, discovery starts

    3. **Discovery Progress Test:**
       - During discovery
       - **Expected:** See "Found X folders" with progress indicator
       - Wait for completion

    4. **Completion Test:**
       - After discovery completes
       - **Expected:** See success message, then auto-navigate to main

    5. **Persistence Test:**
       - On main screen, kill app
       - Restart app
       - **Expected:** Opens directly to main screen (onboarding skipped)

    6. **Error Handling Test:**
       - Clear app data, restart
       - Tap "Grant Access"
       - Press back (cancel picker)
       - **Expected:** Show error with "Try Again" option
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass, or describe any issues.
  </resume-signal>
</task>

</tasks>

<verification>
**Overall Phase Checks:**

1. **Single-screen onboarding:** No wizard steps, tabs, or pages
2. **Permission context:** User sees explanation before SAF picker
3. **SAF integration:** Uses file_picker for ACTION_OPEN_DOCUMENT_TREE
4. **Folder discovery:** Progress shown, system folders filtered
5. **State persistence:** Onboarding completion saved to preferences
6. **Navigation:** Routes from onboarding to main correctly
7. **Error handling:** Permission denial shows retry option

**Test Commands:**
```bash
flutter build apk --debug
adb install -r build/app/outputs/flutter-apk/app-debug.apk
adb shell am start -n com.example.photo_classifier/.MainActivity
```
</verification>

<success_criteria>
- [ ] Onboarding is single-screen with clear value proposition
- [ ] Permission request shows context before SAF picker
- [ ] Folder discovery scans Pictures/ and shows progress
- [ ] Discovered folders persisted with URI permissions
- [ ] Onboarding completion saved to SharedPreferences
- [ ] Navigation handles onboarding -> main flow correctly
- [ ] Permission denial shows error with retry option
- [ ] Manual verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-safety/01-04-SUMMARY.md` with:
- Onboarding flow implementation
- State management approach
- Permission handling details
- UX decisions made (spinner, success confirmation)
- Manual verification results
</output>
