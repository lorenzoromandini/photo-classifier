---
phase: 01-foundation-data-safety
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - lib/data/services/folder_discovery_service.dart
  - lib/data/repositories/folder_repository.dart
  - lib/data/platform/saf_service.dart
  - lib/domain/models/folder_model.dart
  - lib/presentation/providers/folder_provider.dart
  - android/app/src/main/kotlin/com/example/photo_classifier/SafHelper.kt
autonomous: true
user_setup: []

must_haves:
  truths:
    - "App can discover folders in Pictures/ directory via SAF"
    - "SAF URIs are persisted with takePersistableUriPermission()"
    - "Folder discovery skips system folders like Android/"
    - "Photo count per folder is calculated correctly"
    - "Repository exposes discovered folders as reactive state"
  artifacts:
    - path: "lib/data/platform/saf_service.dart"
      provides: "SAF folder discovery and URI operations"
      min_lines: 80
    - path: "lib/data/repositories/folder_repository.dart"
      provides: "Offline-first folder repository"
      min_lines: 60
    - path: "lib/presentation/providers/folder_provider.dart"
      provides: "Riverpod state management for folders"
      min_lines: 40
  key_links:
    - from: "SafService"
      to: "Platform Channel"
      via: "MethodChannel.invokeMethod() for SAF operations"
      pattern: "invokeMethod.*saf"
    - from: "FolderRepository"
      to: "DatabaseService"
      via: "CRUD operations on folders table"
      pattern: "databaseService\\.insertFolder"
---

<objective>
Implement Storage Access Framework (SAF) integration for scoped storage compliance. Create SafService using platform channels for folder discovery and URI permission management. Build FolderRepository following offline-first pattern to sync discovered folders to local database.

Purpose: Required for FILE-01 (SAF permissions) and CAT-01 (folder-based categories). Without SAF, app cannot access user photos on Android 10+.

Output:
1. SafService with folder discovery via platform channel
2. FolderRepository with offline-first pattern
3. FolderProvider for reactive UI state
4. Native Android SAF helper for DocumentFile operations
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-safety/01-CONTEXT.md
@.planning/phases/01-foundation-data-safety/01-01-SUMMARY.md

# Current Flutter implementation
@/home/lromandini/projects/photo-classificator/lib/data/services/folder_discovery_service.dart
@/home/lromandini/projects/photo-classificator/lib/domain/models/folder_model.dart
@/home/lromandini/projects/photo-classificator/lib/presentation/providers/folder_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Native SAF Helper and Update Platform Channel</name>
  <files>
    android/app/src/main/kotlin/com/example/photo_classifier/SafHelper.kt
    android/app/src/main/kotlin/com/example/photo_classifier/PlatformChannelHandler.kt
    android/app/src/main/AndroidManifest.xml
  </files>
  <action>
    Create native Android SAF wrapper for DocumentFile operations:

    1. Create `SafHelper.kt` with SAF utility methods:
       - discoverFolders(baseTreeUri: Uri): List<Map<String, Any>> - returns folder metadata
       - countPhotos(folderUri: Uri): Int - counts image files
       - persistPermission(uri: Uri) - calls contentResolver.takePersistableUriPermission()
       - hasPersistablePermission(uri: Uri): Boolean
       - listPhotos(folderUri: Uri): List<Map<String, Any>> - returns photo URIs and metadata
       - isSystemFolder(name: String): Boolean - checks blacklist

    2. Update `PlatformChannelHandler.kt` to use SafHelper:
       - Add methods: discoverFolders, persistPermission, hasPermission, listPhotos
       - Handle ACTION_OPEN_DOCUMENT_TREE intent for folder picker
       - Return serializable Map objects to Flutter
       - Proper error handling with error codes

    3. Update `AndroidManifest.xml`:
       - Add READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE permissions
       - Add android:preserveLegacyExternalStorage="true" for migration
       - Ensure android:requestLegacyExternalStorage="false" for Android 10+

    System folder blacklist (per 01-CONTEXT.md):
    - Android
    - .thumbnails
    - .trash
    - lost+found
    - LOST.DIR
    - Any folder starting with "."

    Image extensions supported: jpg, jpeg, png, webp, heic, heif
  </action>
  <verify>
    Run `flutter build apk --debug` successfully.
    Check Android native compilation:
    - cd android && ./gradlew compileDebugKotlin
    Verify no compilation errors in SafHelper.kt
  </verify>
  <done>
    SafHelper has all SAF methods. Platform channel exposes them to Flutter.
    Android manifest has proper permissions.
    System folders are filtered in native code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SafService for Flutter</name>
  <files>
    lib/data/platform/saf_service.dart
    lib/data/platform/result.dart
  </files>
  <action>
    Create Flutter service wrapping platform channel SAF operations:

    1. Create `result.dart` for type-safe error handling:
       ```dart
       abstract class Result<T> {
         const Result();
         factory Result.success(T data) = Success<T>;
         factory Result.failure(String error) = Failure<T>;
       }
       class Success<T> extends Result<T> { final T data; ... }
       class Failure<T> extends Result<T> { final String error; ... }
       ```

    2. Create `saf_service.dart`:
       - SafService class with MethodChannel to "com.photo_classifier/platform"
       - Future<Result<List<FolderModel>>> discoverFolders() - calls platform channel
       - Future<Result<void>> persistPermission(String uri)
       - Future<Result<bool>> hasPermission(String uri)
       - Future<Result<List<PhotoModel>>> listPhotos(String folderUri)
       - Future<Result<int>> countPhotos(String folderUri)
       - All methods handle PlatformException and return Result.failure()

    3. Replace existing `FolderDiscoveryService` usage:
       - Deprecate folder_discovery_service.dart (uses dart:io directly, not SAF)
       - Update imports to use SafService
       - Ensure proper async/await patterns

    Implements:
    - FILE-01: SAF obtains permissions for target folders
    - ERR-03: Permission denials trigger re-onboarding (handled via Result.failure)
  </action>
  <verify>
    Create test in onboarding_screen.dart temporarily:
    ```dart
    final safService = SafService();
    final result = await safService.discoverFolders();
    result.when(
      success: (folders) => print('Found ${folders.length} folders'),
      failure: (error) => print('Error: $error'),
    );
    ```
    Run on Android device/emulator, verify folders discovered from Pictures/
  </verify>
  <done>
    SafService exposes all SAF operations via platform channel.
    Result<T> pattern used for error handling.
    All methods handle PlatformException gracefully.
    Folders discovered via SAF, not dart:io.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create FolderRepository with Offline-First Pattern</name>
  <files>
    lib/data/repositories/folder_repository.dart
    lib/data/repositories/result_repository.dart
    lib/presentation/providers/folder_provider.dart
  </files>
  <action>
    Create FolderRepository following offline-first pattern with Riverpod:

    1. Create `folder_repository.dart`:
       - FolderRepository class with constructor injection of DatabaseService and SafService
       - Stream<List<FolderModel>> get folders - emits from database
       - Future<Result<void>> discoverAndSyncFolders() - SAF discovery → database sync
       - Future<Result<void>> persistFolderPermission(String uri)
       - Future<bool> hasPermission(String uri)
       - Future<void> updateLearningStatus(String uri, String status)
       - Future<void> updateLearnedLabels(String uri, Map<String, double> labels)

    2. Implement offline-first pattern:
       - discoverAndSyncFolders: 
         1. Call safService.discoverFolders()
         2. On success: delete existing folders, insert new ones
         3. Return success/failure
       - folders stream: always reads from database (source of truth)
       - SAF is data source, database is source of truth

    3. Update `folder_provider.dart`:
       - Use Riverpod StateNotifier or AsyncNotifier
       - Expose foldersProvider: StreamProvider for reactive list
       - Expose folderActionsProvider for mutations
       - Handle loading states properly

    Learning statuses:
    - pending: Not yet processed
    - in_progress: Currently learning
    - completed: Learning finished

    Per user decision (01-CONTEXT.md):
    - Include all folders in Pictures/ by default
    - System folders filtered by SafHelper
  </action>
  <verify>
    Run Flutter app, test flow:
    1. Call folderRepository.discoverAndSyncFolders()
    2. Verify folders appear in UI via folderProvider
    3. Check database via adb: SELECT * FROM folders
    4. Kill app, restart - folders should persist
    5. Verify system folders (Android, .thumbnails) NOT in database
  </verify>
  <done>
    FolderRepository syncs SAF folders to database.
    Offline-first pattern working: DB is source of truth.
    foldersProvider exposes reactive stream.
    System folders filtered, permissions persisted.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**

1. **SAF Discovery:** Run app, verify folders from Pictures/ discovered
2. **Permission Persistence:** Grant permission, kill app, restart - should still have access
3. **System Folder Filtering:** Check Android/, .thumbnails not in list
4. **Offline-First:** Enable airplane mode, folders still show from database
5. **Photo Count:** Verify photo counts are accurate per folder

**Test Commands:**
```bash
flutter build apk --debug
adb install build/app/outputs/flutter-apk/app-debug.apk
adb shell run-as com.example.photo_classifier sqlite3 databases/photo_classifier.db "SELECT * FROM folders;"
```
</verification>

<success_criteria>
- [ ] SafService discovers folders via platform channel
- [ ] Native SafHelper filters system folders
- [ ] URI permissions persisted and survive app restart
- [ ] FolderRepository implements offline-first pattern
- [ ] foldersProvider exposes reactive stream to UI
- [ ] Photo counts calculated correctly per folder
- [ ] Error handling via Result<T> pattern
- [ ] No system folders in discovered list
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-safety/01-02-SUMMARY.md` with:
- SafService methods and native SAF implementation
- FolderRepository offline-first pattern details
- FolderProvider state management
- Any deviations or decisions made
</output>
