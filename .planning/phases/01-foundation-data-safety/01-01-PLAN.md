---
phase: 01-foundation-data-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/kotlin/com/example/photo_classifier/MainActivity.kt
  - android/app/src/main/kotlin/com/example/photo_classifier/PlatformChannelHandler.kt
  - lib/data/database/database_service.dart
  - lib/data/services/preference_service.dart
  - lib/data/database/entities/folder_entity.dart
  - lib/data/database/entities/category_entity.dart
  - lib/data/database/entities/photo_entity.dart
  - lib/data/database/entities/transaction_entity.dart
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Flutter can communicate with Android native via platform channels"
    - "Database tables exist for folders, categories, photos, and transactions"
    - "User preferences persist across app restarts"
    - "Database migration strategy handles schema changes"
  artifacts:
    - path: "android/app/src/main/kotlin/com/example/photo_classifier/PlatformChannelHandler.kt"
      provides: "Method channel for Flutter-Android communication"
      min_lines: 50
    - path: "lib/data/database/database_service.dart"
      provides: "SQLite database with sqflite"
      min_lines: 100
    - path: "lib/data/services/preference_service.dart"
      provides: "SharedPreferences wrapper"
      min_lines: 40
  key_links:
    - from: "lib/data/database/database_service.dart"
      to: "sqflite package"
      via: "Database instance management"
      pattern: "openDatabase|getDatabasePath"
    - from: "lib/data/services/preference_service.dart"
      to: "shared_preferences package"
      via: "SharedPreferences.getInstance()"
      pattern: "SharedPreferences"
---

<objective>
Establish the foundation for Flutter-Android communication and data persistence.

Purpose: This is the bedrock of the entire app. Platform channels enable SAF operations (required for Android 10+ scoped storage), while the database stores photo metadata and the transaction log for crash recovery. Without this foundation, no file operations can be safe.

Output:
1. Platform channel infrastructure (Flutter + Kotlin)
2. Database entities and service for sqflite
3. Preference service for user settings
4. Migration strategy for schema evolution
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-safety/01-CONTEXT.md

# Current Flutter project structure
@/home/lromandini/projects/photo-classificator/pubspec.yaml
@/home/lromandini/projects/photo-classificator/lib/main.dart
@/home/lromandini/projects/photo-classificator/lib/data/database/database_service.dart
@/home/lromandini/projects/photo-classificator/lib/data/services/preference_service.dart

# Research findings
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Platform Channel Infrastructure</name>
  <files>
    android/app/src/main/kotlin/com/example/photo_classifier/MainActivity.kt
    android/app/src/main/kotlin/com/example/photo_classifier/PlatformChannelHandler.kt
    lib/data/platform/platform_channel_service.dart
  </files>
  <action>
    Create the Flutter-Android communication bridge for SAF operations:

    1. Create `PlatformChannelHandler.kt` in Android native with:
       - MethodChannel for "com.photo_classifier/platform" 
       - Methods: requestFolderPermission, getPersistedPermissionUris, copyFileViaSaf, moveFileViaSaf, deleteFileViaSaf, verifyFileExists, getFileSize
       - Proper error handling with Result.error() for failed operations
       - SAF DocumentFile operations using ContentResolver

    2. Update `MainActivity.kt` to register the handler:
       ```kotlin
       class MainActivity : FlutterActivity() {
         override fun configureFlutterEngine(flutterEngine: FlutterEngine) {
           super.configureFlutterEngine(flutterEngine)
           PlatformChannelHandler(this, flutterEngine.dartExecutor.binaryMessenger)
         }
       }
       ```

    3. Create `platform_channel_service.dart` in Flutter:
       - static const MethodChannel _channel = MethodChannel('com.photo_classifier/platform')
       - Methods mirroring native: requestFolderPermission(), copyFile(), moveFile(), deleteFile(), verifyFile(), getFileSize()
       - Proper error handling with try/catch and PlatformException
       - Result<T> pattern for type-safe error propagation (Success/Failure)

    Use the existing file_picker package for initial folder selection, but this channel
    is needed for the actual file operations via SAF (file_picker doesn't handle moves).
  </action>
  <verify>
    Run `flutter build apk` successfully. Create a test widget that calls:
    - platformChannelService.requestFolderPermission() - should show SAF picker
    - platformChannelService.verifyFileExists("content://...") - should return Result<bool>
    Check Android Studio logcat for "PlatformChannelHandler" initialization messages.
  </verify>
  <done>
    Platform channel methods callable from Flutter, returning Result<T> pattern. 
    SAF operations can be initiated from Flutter and handled in native Android.
    All methods have try/catch with Result.error() for native and PlatformException for Flutter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Database Schema and Entities</name>
  <files>
    lib/data/database/database_service.dart
    lib/data/database/entities/folder_entity.dart
    lib/data/database/entities/category_entity.dart
    lib/data/database/entities/photo_entity.dart
    lib/data/database/entities/transaction_entity.dart
    lib/data/database/entities/trash_entity.dart
  </files>
  <action>
    Enhance the existing database_service.dart with proper entity classes and schema:

    1. Create entity classes in lib/data/database/entities/:
       - folder_entity.dart: FolderEntity with fields (id, uri, name, displayName, photoCount, isActive, learnedLabels JSON, learningStatus, createdAt)
       - category_entity.dart: CategoryEntity with fields (id, name, targetFolderUri, mlLabels JSON, confidenceThreshold, createdAt)
       - photo_entity.dart: PhotoEntity with fields (uri, folderUri, fileName, fileSize, processedAt, detectedLabels JSON, status, targetCategoryId)
       - transaction_entity.dart: TransactionEntity with fields (id, sourceUri, destUri, operationType, status, createdAt, completedAt, retryCount, errorMessage)
       - trash_entity.dart: TrashEntity with fields (originalUri, trashUri, fileName, fileSize, movedAt, expiresAt, restored)

    2. Update database_service.dart:
       - Add JSON serialization helpers for learnedLabels, mlLabels, detectedLabels
       - Add foreign key constraints: photos.folder_uri -> folders.uri (ON DELETE CASCADE)
       - Add indexes for frequently queried columns (folder_uri, status, expires_at)
       - Implement database migration strategy (onUpgrade handler)

    3. Convert existing models to entities:
       - Update FolderModel, PhotoModel, TransactionModel to use entity classes
       - Add fromEntity() and toEntity() methods on models

    Schema must support:
    - CAT-01, CAT-02: Categories with target folders (max 10)
    - CAT-05: Configuration persistence via DataStore (preferences layer)
    - FILE-03: Transaction log tracks operations
    - FILE-05: 7-day trash folder with expiration
    - DATA-01, DATA-02: Photo metadata and processing tracking
    - DATA-03: Processing queue (status = 'pending')
    - DATA-04: Storage full detection (file_size tracking)
  </action>
  <verify>
    Run Flutter app, check database creation:
    - adb shell run-as com.example.photo_classifier ls databases/
    - Verify photo_classifier.db exists
    - Run: SELECT name FROM sqlite_master WHERE type='table' - should return folders, photos, transactions, categories, trash
    - Test CRUD operations on each table via database service
  </verify>
  <done>
    All 5 tables created with proper schema. Foreign keys and indexes in place.
    Database service provides CRUD operations for all entities.
    JSON serialization working for label maps.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Preference Service with Type Safety</name>
  <files>
    lib/data/services/preference_service.dart
    lib/domain/models/preference_keys.dart
  </files>
  <action>
    Enhance the existing PreferenceService to support all Phase 1 settings:

    1. Create preference_keys.dart with constants:
       ```dart
       class PreferenceKeys {
         static const String onboardingComplete = 'onboarding_complete';
         static const String confidenceThreshold = 'confidence_threshold';
         static const String folderUris = 'folder_uris';
         static const String categories = 'categories';
         static const String isFirstLaunch = 'is_first_launch';
         static const String lastLearningRun = 'last_learning_run';
       }
       ```

    2. Update preference_service.dart:
       - Add get/set for confidenceThreshold (double, default 0.9 - High per user decision)
       - Add get/set for categories (JSON encoded List<CategoryModel>)
       - Add get/set for lastLearningRun (int timestamp)
       - Add getCategoryCount() returning int (enforce CAT-02: max 10)
       - Add isCategoryLimitReached() helper
       - Add validation: setConfidenceThreshold rejects values outside 0.0-1.0

    3. Add preference migration helper:
       - handlePreferenceMigration() for future schema changes
       - Version tracking in preferences

    Implements:
    - CAT-02: Maximum 10 categories (enforced in preference service)
    - CAT-05: Configuration persists via SharedPreferences
  </action>
  <verify>
    Create test in main.dart (temporarily):
    ```dart
    final prefs = PreferenceService();
    await prefs.init();
    await prefs.setConfidenceThreshold(0.9);
    print(await prefs.getConfidenceThreshold()); // Should print 0.9
    await prefs.addFolderUri('content://...');
    print(await prefs.getFolderUris()); // Should include content://...
    ```
    Kill and restart app, verify values persist.
  </verify>
  <done>
    Preference service stores and retrieves all settings. 
    Confidence threshold defaults to 0.9 (High).
    Category limit of 10 enforced.
    All values persist across app restarts.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**

1. **Platform Channel:** Run `flutter build apk --debug`, verify no compilation errors in Android native
2. **Database:** Check tables exist with proper schema via adb shell sqlite3
3. **Preferences:** Kill and restart app, verify settings persist
4. **Integration:** Create a test that calls platform channel -> stores result in database -> reads via preference

**Run commands:**
```bash
flutter analyze
flutter test
flutter build apk
```

**Manual verification:**
- Install APK on device
- Grant permissions when prompted
- Verify database file created
- Change confidence threshold in settings
- Kill app, restart - threshold should persist
</verification>

<success_criteria>
- [ ] Platform channel methods callable from Flutter with Result<T> pattern
- [ ] Database creates all 5 tables (folders, photos, transactions, categories, trash)
- [ ] Foreign keys and indexes configured
- [ ] Preference service persists all settings across restarts
- [ ] Confidence threshold defaults to 0.9 (High)
- [ ] Category limit of 10 enforced
- [ ] No lint errors, app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-safety/01-01-SUMMARY.md` with:
- Platform channel methods implemented
- Database schema with all entities
- Preference service with all settings
- Any deviations or decisions made
</output>
