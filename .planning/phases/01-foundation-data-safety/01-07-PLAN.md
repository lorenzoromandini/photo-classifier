---
phase: 01-foundation-data-safety
plan: 07
type: execute
wave: 3
depends_on: ["01-04", "01-05", "01-06"]
files_modified:
  - app/src/main/java/com/example/photoorganizer/ui/main/MainScreen.kt
  - app/src/main/java/com/example/photoorganizer/ui/main/MainViewModel.kt
  - app/src/main/java/com/example/photoorganizer/ui/settings/SettingsScreen.kt
  - app/src/main/java/com/example/photoorganizer/ui/settings/SettingsViewModel.kt
  - app/src/main/java/com/example/photoorganizer/ui/components/FolderList.kt
  - app/src/main/java/com/example/photoorganizer/ui/components/ConfidenceSlider.kt
  - app/src/main/java/com/example/photoorganizer/data/repository/RecoveryStartup.kt
  - app/src/main/java/com/example/photoorganizer/PhotoOrganizerApplication.kt
autonomous: false
must_haves:
  truths:
    - "Main screen shows discovered folders and learning status"
    - "Settings allows confidence threshold adjustment (0.6/0.75/0.9)"
    - "App recovers from crashes by replaying transaction log on startup"
    - "Storage full warnings shown to user"
    - "Permission denial triggers re-onboarding"
  artifacts:
    - path: "app/src/main/java/com/example/photoorganizer/PhotoOrganizerApplication.kt"
      provides: "Application class with recovery on startup"
      exports: ["PhotoOrganizerApplication"]
    - path: "app/src/main/java/com/example/photoorganizer/ui/main/MainScreen.kt"
      provides: "Main UI with folder list"
      exports: ["MainScreen"]
    - path: "app/src/main/java/com/example/photoorganizer/ui/settings/SettingsScreen.kt"
      provides: "Settings with confidence threshold"
      exports: ["SettingsScreen"]
  key_links:
    - from: "PhotoOrganizerApplication.onCreate()"
      to: "TransactionRepository.recoverPendingOperations()"
      via: "recovery on every app startup"
    - from: "SettingsScreen"
      to: "UserPreferencesRepository"
      via: "saves confidence threshold (0.6/0.75/0.9)"
---

<objective>
Build main screen showing discovered folders and learning progress. Create settings screen for confidence threshold (0.6/0.75/0.9 per user decision). Implement crash recovery on app startup. Handle permission denials and storage warnings.

Purpose: Completes Phase 1 success criteria: categories persist, permissions granted, crash recovery works, storage handled.
Output: Full Phase 1 UI with recovery and settings.
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-safety/01-RESEARCH.md
@.planning/phases/01-foundation-data-safety/01-CONTEXT.md
@.planning/phases/01-foundation-data-safety/01-01-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-03-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-04-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-05-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Application class with startup recovery</name>
  <files>
    app/src/main/java/com/example/photoorganizer/PhotoOrganizerApplication.kt
    app/src/main/java/com/example/photoorganizer/data/repository/RecoveryStartup.kt
  </files>
  <action>
    Create Application class with crash recovery on startup:

    1. **PhotoOrganizerApplication class**:
       - @HiltAndroidApp
       - Extend Application()
       - Inject TransactionRepository (via lateinit or entry point)
       - override fun onCreate()

    2. **RecoveryStartup** (separate class for testability):
       - @Inject constructor with transactionRepository: TransactionRepository
       - suspend fun initialize() - called from Application.onCreate
       - Calls transactionRepository.recoverPendingOperations()
       - Logs result: "Recovered X operations, Y failed"

    3. **Startup flow**:
       ```
       - Initialize Timber for logging
       - Check if onboarding completed (from preferences)
       - If yes: run RecoveryStartup.initialize()
       - Schedule TrashCleanupWorker if not scheduled
       - Log app startup event
       ```

    4. **Error handling**:
       - Recovery failures logged, don't crash app
       - Failed operations remain in PENDING for next attempt
       - Coroutine launched in GlobalScope for recovery

    Implementation notes:
       - Use @EntryPoint for injecting into Application
       - Recovery must complete before any file operations
       - Timber planted in DEBUG builds only
  </action>
  <verify>
    Run: `./gradlew build`
    Check: Application class compiles with HiltAndroidApp
  </verify>
  <done>
    Application class runs recovery on startup, initializes logging
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MainScreen with folder list</name>
  <files>
    app/src/main/java/com/example/photoorganizer/ui/components/FolderList.kt
    app/src/main/java/com/example/photoorganizer/ui/components/FolderCard.kt
    app/src/main/java/com/example/photoorganizer/ui/main/MainViewModel.kt
    app/src/main/java/com/example/photoorganizer/ui/main/MainScreen.kt
  </files>
  <action>
    Create main screen showing discovered folders:

    1. **FolderCard composable**:
       - Takes Folder domain model
       - Shows folder name, photo count
       - Shows learning status (PENDING/IN_PROGRESS/COMPLETED)
       - Shows learned categories (if completed)
       - Material 3 Card with elevation

    2. **FolderList composable**:
       - Takes List<Folder>
       - LazyColumn for performance
       - Empty state: "No folders discovered yet"
       - Group by learning status

    3. **MainViewModel**:
       - @HiltViewModel
       - Inject FolderRepository
       - val folders: StateFlow<List<Folder>> from repository
       - val isLoading: StateFlow<Boolean>
       - fun refreshFolders() - trigger re-sync
       - Check permission status, navigate to onboarding if lost

    4. **MainScreen composable**:
       - Top app bar with title "Photo Organizer"
       - Settings icon in top bar (navigates to settings)
       - FolderList in main content
       - FAB or pull-to-refresh for manual sync
       - Show permission warning if permissions lost

    5. **Empty/Loading states**:
       - CircularProgressIndicator while loading
       - Message if no folders ("Complete onboarding to discover folders")
       - Error state with retry button

    Navigation:
       - Settings icon -> SettingsScreen
       - Back from settings -> MainScreen
  </action>
  <verify>
    Run: `./gradlew compileDebugKotlin`
    Check: Previews compile, no navigation errors
  </verify>
  <done>
    MainScreen displays folders with status, has settings navigation
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SettingsScreen with confidence threshold</name>
  <files>
    app/src/main/java/com/example/photoorganizer/ui/components/ConfidenceSlider.kt
    app/src/main/java/com/example/photoorganizer/ui/settings/SettingsViewModel.kt
    app/src/main/java/com/example/photoorganizer/ui/settings/SettingsScreen.kt
  </files>
  <action>
    Create settings screen with confidence threshold (user decision: 0.6/0.75/0.9):

    1. **ConfidenceSlider composable**:
       - Three preset buttons: Low (0.6), Medium (0.75), High (0.9)
       - Visual indicator of current selection
       - Description text for each level:
         - Low: "More photos organized, may include mistakes"
         - Medium: "Balanced approach"
         - High (default): "Conservative, fewer mistakes"
       - Material 3 SegmentedButton or custom row

    2. **SettingsViewModel**:
       - @HiltViewModel
       - Inject UserPreferencesRepository
       - val confidenceThreshold: StateFlow<Float>
       - val folders: StateFlow<List<Folder>> (for trash view)
       - fun setConfidenceThreshold(value: Float)
       - fun getTrashItems(): Flow<List<TrashItem>>
       - fun restoreFromTrash(item: TrashItem)
       - fun clearAllData() - for debugging/support

    3. **SettingsScreen sections**:
       - **Organization**: Confidence threshold selector
       - **Folders**: List of configured folders with learned categories
       - **Trash**: View items in trash (optional for v1)
       - **About**: App version, privacy policy link

    4. **Settings UI**:
       - Top app bar with back button
       - LazyColumn with sections
       - Section headers with Material 3 ListHeader
       - Divider between sections

    5. **Persistence**:
       - Threshold saves immediately on selection
       - Shows "Saved" toast/snackbar briefly

    User decision: Default is High (0.9)
  </action>
  <verify>
    Run: `./gradlew build`
    Check: Settings screen compiles with all components
  </verify>
  <done>
    SettingsScreen has confidence selector (0.6/0.75/0.9), shows folders
  </done>
</task>

</tasks>

<verification>
[ ] App runs recovery on startup (transaction log replay)
[ ] MainScreen shows discovered folders with learning status
[ ] Settings has confidence threshold selector (Low/Medium/High)
[ ] Default threshold is High (0.9) per user decision
[ ] Permission loss triggers re-onboarding flow
[ ] Storage warnings shown when space is low
</verification>

<success_criteria>
- PhotoOrganizerApplication runs TransactionRepository.recoverPendingOperations() on startup
- MainScreen displays folders with learning status (PENDING/IN_PROGRESS/COMPLETED)
- Settings allows confidence threshold selection: Low (0.6), Medium (0.75), High (0.9)
- Default threshold is 0.9 (High) as per user decision
- Permission denial navigates back to onboarding
- Storage full detection shows warning to user
- All Phase 1 requirements implemented and verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-safety/01-07-SUMMARY.md`
</output>
