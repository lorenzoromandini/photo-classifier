---
phase: 01-foundation-data-safety
plan: 07
type: execute
wave: 3
depends_on:
  - 01-04
  - 01-05
  - 01-06
files_modified:
  - lib/main.dart
  - lib/presentation/screens/main_screen.dart
  - lib/presentation/screens/settings_screen.dart
  - lib/presentation/providers/main_provider.dart
  - lib/presentation/providers/settings_provider.dart
  - lib/presentation/widgets/folder_list.dart
  - lib/presentation/widgets/confidence_selector.dart
  - lib/data/services/startup_service.dart
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Main screen shows discovered folders and learning status"
    - "Settings allows confidence threshold adjustment (0.6/0.75/0.9)"
    - "App recovers from crashes by replaying transaction log on startup"
    - "Storage full warnings shown to user"
    - "Permission denial triggers re-onboarding"
  artifacts:
    - path: "lib/data/services/startup_service.dart"
      provides: "Startup recovery and initialization"
      min_lines: 50
    - path: "lib/presentation/screens/main_screen.dart"
      provides: "Main UI with folder list"
      min_lines: 80
    - path: "lib/presentation/screens/settings_screen.dart"
      provides: "Settings with confidence threshold"
      min_lines: 100
  key_links:
    - from: "StartupService.initialize()"
      to: "TransactionRepository.recoverPendingOperations()"
      via: "recovery on every app startup"
    - from: "SettingsScreen"
      to: "PreferenceService"
      via: "saves confidence threshold (0.6/0.75/0.9)"
---

<objective>
Build main screen showing discovered folders and learning progress. Create settings screen for confidence threshold (0.6/0.75/0.9 per user decision). Implement crash recovery on app startup. Handle permission denials and storage warnings.

Purpose: Completes Phase 1 success criteria: categories persist, permissions granted, crash recovery works, storage handled.

Output:
1. StartupService for recovery on app launch
2. MainScreen with folder list and learning status
3. SettingsScreen with confidence threshold selector
4. Recovery and permission handling
</objective>

<execution_context>
@/home/lromandini/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/lromandini/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-safety/01-CONTEXT.md
@.planning/phases/01-foundation-data-safety/01-01-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-03-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-04-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-05-SUMMARY.md
@.planning/phases/01-foundation-data-safety/01-06-SUMMARY.md

# Existing Flutter main screen
@/home/lromandini/projects/photo-classificator/lib/presentation/screens/main_screen.dart
@/home/lromandini/projects/photo-classificator/lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StartupService with Crash Recovery</name>
  <files>
    lib/data/services/startup_service.dart
    lib/main.dart
  </files>
  <action>
    Create StartupService for app initialization and crash recovery:

    1. Create `startup_service.dart`:
       - StartupService with dependencies:
         - TransactionRepository (for recovery)
         - PreferenceService (for settings)
         - SafeFileService (for storage check)
       - Methods:
         - Future<void> initialize() - main initialization
         - Future<RecoveryResult> recoverPendingOperations()
           - Calls TransactionRepository.recoverPendingOperations()
           - Logs result: "Recovered X operations, Y failed"
           - Returns RecoveryResult
         - Future<void> scheduleBackgroundWorkers()
           - Schedules TrashCleanupWorker (from 01-06)
           - Schedules FolderLearningWorker (from 01-05) if needed
         - Future<bool> checkPermissions() - verifies SAF permissions still valid
         - Future<StorageInfo> checkStorage() - checks available space
       
       Implementation:
       ```dart
       Future<void> initialize() async {
         // Run recovery first (before any file operations)
         final recoveryResult = await recoverPendingOperations();
         if (recoveryResult.failed > 0) {
           // Log failed recoveries
         }
         
         // Schedule background workers
         await scheduleBackgroundWorkers();
         
         // Check storage
         final storage = await checkStorage();
         if (storage.state == StorageState.critical) {
           // Will be shown to user via state
         }
       }
       ```

    2. Update `main.dart`:
       - Call startupService.initialize() before runApp()
       - Handle recovery errors gracefully
       - Pass storage state to app if needed
       - Navigation logic:
         ```dart
         final prefs = PreferenceService();
         await prefs.init();
         final onboardingComplete = prefs.getOnboardingComplete();
         
         if (onboardingComplete) {
           // Run startup recovery
           final startup = StartupService(...);
           await startup.initialize();
           
           // Check if permissions still valid
           final hasPermissions = await startup.checkPermissions();
           if (!hasPermissions) {
             // Reset onboarding to re-request permissions (ERR-03)
             await prefs.setOnboardingComplete(false);
           }
         }
         ```

    3. Recovery strategy (per user decision - 01-CONTEXT.md):
       - Transaction log replay triggers on startup
       - Recovery visibility: Silent (no toast, log only)
       - Failed operations: Logged, remain for next attempt

    Implements:
    - ERR-03: Permission denials trigger re-onboarding
    - ERR-04: Transaction log enables crash recovery
    - DATA-04: Storage full detection
  </action>
  <verify>
    Test startup:
    1. Create pending transaction in database
    2. Kill app mid-operation (simulate crash)
    3. Restart app
    4. Check logs: "Recovered X operations"
    5. Verify transaction completed to status=completed
  </verify>
  <done>
    StartupService runs recovery on app launch.
    Background workers scheduled.
    Permissions checked, triggers re-onboarding if lost.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MainScreen with Folder List</name>
  <files>
    lib/presentation/screens/main_screen.dart
    lib/presentation/widgets/folder_list.dart
    lib/presentation/widgets/folder_card.dart
    lib/presentation/providers/main_provider.dart
  </files>
  <action>
    Update existing MainScreen with complete folder list:

    1. Create `folder_card.dart`:
       - FolderCard widget with:
         - Folder name (bold)
         - Photo count: "X photos"
         - Learning status badge:
           - pending: Grey "Learning Pending"
           - in_progress: Blue "Learning..."
           - completed: Green "Ready"
         - Learned labels (if completed): show top 3 labels
       - Material 3 Card with elevation
       - Trailing icon based on status

    2. Create `folder_list.dart`:
       - FolderList widget:
         - Takes List<FolderModel>
         - ListView.builder for performance
         - Empty state: "No folders discovered"
         - Pull-to-refresh for manual sync
         - Group by learning status (completed first)

    3. Create `main_provider.dart`:
       - MainState class:
         ```dart
         class MainState {
           final List<FolderModel> folders;
           final bool isLoading;
           final String? error;
           final StorageState? storageState;
           final bool needsReOnboarding;
         }
         ```
       - MainNotifier extends StateNotifier<MainState>:
         - loadFolders() - fetches from FolderRepository
         - refreshFolders() - triggers re-discovery
         - checkPermissions() - verifies SAF permissions
         - Methods for manual actions
       - Provider: mainProvider = StateNotifierProvider(...)

    4. Update `main_screen.dart`:
       - Scaffold with AppBar:
         - Title: "Photo Classifier"
         - Actions: [IconButton for settings]
       - Body: FolderList with folders from provider
       - FAB: Refresh button (optional)
       - Show storage warning if StorageState.critical
       - Show permission banner if needsReOnboarding
       - Navigation to settings on settings icon tap

    5. Empty/Loading states:
       - CircularProgressIndicator while loading
       - "No folders yet. Complete onboarding to discover folders."
       - Error with retry button

    Per user decision (Claude's discretion):
    - Learning presentation: Progress via status badges
    - Folder selection: Include all folders by default
  </action>
  <verify>
    Run app, verify:
    1. Main screen shows folder list
    2. Each folder shows name, photo count, learning status
    3. Settings icon navigates to settings
    4. Pull-to-refresh works
    5. Empty state shown if no folders
  </verify>
  <done>
    MainScreen displays folders with learning status.
    FolderCard shows relevant info.
    Navigation to settings working.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SettingsScreen with Confidence Selector</name>
  <files>
    lib/presentation/screens/settings_screen.dart
    lib/presentation/widgets/confidence_selector.dart
    lib/presentation/providers/settings_provider.dart
  </files>
  <action>
    Create settings screen with confidence threshold (per user decision: 0.6/0.75/0.9):

    1. Create `confidence_selector.dart`:
       - ConfidenceSelector widget:
         - Three preset buttons: Low (0.6), Medium (0.75), High (0.9)
         - SegmentedButton or ToggleButtons from Material 3
         - Visual indicator of current selection
         - Description for each level:
           - Low: "More photos organized, may include mistakes"
           - Medium: "Balanced approach"
           - High (default): "Conservative, fewer mistakes"
         - Selected button highlighted
         - Callback: onChanged(double threshold)

    2. Create `settings_provider.dart`:
       - SettingsState class:
         ```dart
         class SettingsState {
           final double confidenceThreshold;
           final int trashItemCount;
           final String trashSize;
           final bool isLoading;
         }
         ```
       - SettingsNotifier extends StateNotifier<SettingsState>:
         - loadSettings() - reads from PreferenceService
         - setConfidenceThreshold(double value) - saves to preferences
         - getTrashInfo() - loads trash stats
         - emptyTrash() - calls TrashService.emptyTrash()
       - Provider: settingsProvider = StateNotifierProvider(...)

    3. Create `settings_screen.dart`:
       - Scaffold with AppBar:
         - Title: "Settings"
         - Back button (pop)
       - Body: ListView with sections:
         
         **Organization Section:**
         - ListTile with title "Confidence Threshold"
         - Subtitle: "How sure the app must be before organizing"
         - ConfidenceSelector widget
         - Shows current selection
         
         **Folders Section:**
         - ListTile with title "Managed Folders"
         - Shows count of folders
         - Tap to view (optional navigation)
         
         **Trash Section:**
         - ListTile with title "Trash"
         - Subtitle: "${state.trashItemCount} items • ${state.trashSize}"
         - "Empty Trash" button (trailing)
         
         **About Section:**
         - ListTile with title "About"
         - Subtitle: "Photo Classifier v1.0"
         - Optional: Privacy policy link
       
       - Material 3 dividers between sections

    4. Persistence:
       - Confidence saves immediately on selection
       - Show brief "Saved" snackbar

    Per user decision (STATE.md, multiple plans):
    - Default: High (0.9) - Conservative to build trust
    - Three levels: Low (0.6), Medium (0.75), High (0.9)

    Implements:
    - CAT-05: Configuration persists via DataStore
  </action>
  <verify>
    Test settings:
    1. Open settings
    2. Change confidence threshold to Low (0.6)
    3. Kill app, restart
    4. Verify threshold still 0.6 (persisted)
    5. Verify "Saved" snackbar shown
  </verify>
  <done>
    SettingsScreen has confidence selector.
    Three preset levels working.
    Persistence across restarts.
    Trash info displayed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement Permission and Storage Handling</name>
  <files>
    lib/presentation/widgets/permission_banner.dart
    lib/presentation/widgets/storage_warning.dart
    lib/presentation/screens/main_screen.dart
  </files>
  <action>
    Add permission and storage warning UI:

    1. Create `permission_banner.dart`:
       - PermissionBanner widget:
         - Shows when SAF permissions revoked
         - Message: "Photo access permission required"
         - "Grant Access" button
         - Material 3 Banner styling (colored background)
       - On tap: navigate to onboarding

    2. Create `storage_warning.dart`:
       - StorageWarning widget:
         - Shows when storage is low (StorageState.low or critical)
         - Message: "Storage space is running low"
         - Icon: Icons.storage with warning color
         - Material 3 Card with warning color

    3. Update `main_screen.dart`:
       - Show PermissionBanner at top if needsReOnboarding
       - Show StorageWarning if storageState != ok
       - These should be conditional (only when needed)

    4. Handle permission denial (ERR-03):
       - When user denies permission in system dialog
       - Show PermissionBanner with retry option
       - On retry: re-trigger onboarding flow

    5. Handle storage full (DATA-04):
       - When storageState.critical
       - Show persistent warning
       - Disable file operations (prevent crashes)

    Per user decision (Claude's discretion - 01-CONTEXT.md):
    - Permission denial handling: Retry mechanism with banner
    - Storage warning presentation: Banner at top of main screen
  </action>
  <verify>
    Test error scenarios:
    1. Revoke SAF permission manually (Android Settings)
    2. Open app - should show PermissionBanner
    3. Tap "Grant Access" - should go to onboarding
    4. Fill storage (or mock low storage)
    5. Should show StorageWarning
  </verify>
  <done>
    PermissionBanner shows when permissions lost.
    StorageWarning shows when low on space.
    Navigation to onboarding works.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Verify Phase 1 Success Criteria</name>
  <what-built>
    Complete Phase 1 implementation:
    - Platform channels for SAF
    - Database with transaction log
    - Safe file operations with recovery
    - Onboarding flow
    - Folder learning system
    - Trash system
    - Main screen with settings
  </what-built>
  <how-to-verify>
    **Test Phase 1 Success Criteria:**

    1. **User can define and persist categories (CAT-01, CAT-02, CAT-05):**
       - Fresh install, complete onboarding
       - Verify folders discovered (auto-categories)
       - Check folders persist after app restart
       - > Expected: Categories (folders) persist

    2. **Permissions granted through onboarding (FILE-01, ERR-03):**
       - Revoke SAF permission in Android Settings
       - Open app
       - Verify redirected to onboarding
       - Grant permission
       - > Expected: Returns to main screen with folders

    3. **File operations are crash-safe (FILE-02, FILE-03, ERR-04):**
       - Create test scenario with pending transaction
       - Kill app mid-operation (debugger or manual)
       - Restart app
       - Check logs for recovery
       - Verify no data loss
       - > Expected: Recovery runs, operation completes

    4. **Storage constraints handled gracefully (DATA-04):**
       - Mock low storage condition (or fill storage)
       - Attempt file operation
       - Verify warning shown
       - Operation blocked gracefully
       - > Expected: Warning visible, no crash

    **Test Flow:**
    ```bash
    # Fresh install
    adb uninstall com.example.photo_classifier
    flutter build apk --debug
    adb install build/app/outputs/flutter-apk/app-debug.apk
    adb shell am start -n com.example.photo_classifier/.MainActivity
    ```

    **Manual Checks:**
    - [ ] Onboarding completes successfully
    - [ ] Folders appear in main screen
    - [ ] Learning status updates (wait for WorkManager)
    - [ ] Settings shows confidence threshold (default 0.9)
    - [ ] Changing threshold persists
    - [ ] Trash shows items (if any deleted)
    - [ ] No crashes during testing
  </how-to-verify>
  <resume-signal>
    Type "approved" if all Phase 1 success criteria pass, or describe issues.
  </resume-signal>
</task>

</tasks>

<verification>
**Phase 1 Exit Criteria (from STATE.md):**

Before transitioning to Phase 2, verify:

- [ ] User can create and persist 10 categories
- [ ] SAF onboarding grants permissions for target folders
- [ ] Transaction log records all file operations
- [ ] Crash recovery test passes (simulate mid-move crash)
- [ ] Storage full condition handled gracefully
- [ ] All Phase 1 success criteria verified

**Run Tests:**
```bash
flutter analyze
flutter test
flutter build apk --release
```

**Manual Testing Matrix:**
| Test | Device | Expected | Actual |
|------|--------|----------|--------|
| Fresh install | Android 12 | Onboarding shown | |
| Folder discovery | Any | Folders listed | |
| Learning completion | Any | Status=completed | |
| Crash recovery | Any | Recovery runs | |
| Permission revocation | Any | Re-onboarding | |
| Storage warning | Low storage | Warning shown | |
</verification>

<success_criteria>
- [ ] StartupService runs TransactionRepository.recoverPendingOperations() on startup
- [ ] MainScreen displays folders with learning status (PENDING/IN_PROGRESS/COMPLETED)
- [ ] Settings allows confidence threshold selection: Low (0.6), Medium (0.75), High (0.9)
- [ ] Default threshold is 0.9 (High) as per user decision
- [ ] Permission denial navigates back to onboarding
- [ ] Storage full detection shows warning to user
- [ ] All Phase 1 requirements implemented and verified
- [ ] Manual verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-safety/01-07-SUMMARY.md` with:
- Startup recovery implementation
- MainScreen and SettingsScreen features
- Phase 1 success criteria verification results
- Known issues or limitations
- Phase 1 completion summary
</output>
